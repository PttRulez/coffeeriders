export enum Role {
    Admin = 'admin',
    Regular = 'regular'
}

export enum BikeCategory {
    Road = 'road',
    Gravel = 'gravel',
    MTB = 'mtb',
}

export enum Pedals {
    Shimano = 'shimano',
    Look = 'look'
}