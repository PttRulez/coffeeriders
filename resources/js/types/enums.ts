export enum Role {
    Admin = 'admin',
    Regular = 'regular',
}

export enum BikeCategory {
    Road = 'road',
    Gravel = 'gravel',
    MTB = 'mtb',
}

export enum Pedals {
    Shimano = 'shimano',
    Look = 'look',
}

export enum RaceType {
    Gravel = 'gravel',
    Road = 'road',
    MTB = 'mtb',
    Indoor = 'indoor',
    Track = 'track',
    Cyclocross = 'cyclocross',
}
